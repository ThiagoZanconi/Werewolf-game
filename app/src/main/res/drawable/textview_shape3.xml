<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Imagen de fondo -->
    <item>
        <bitmap
            android:src="@drawable/image_wood_background" />
    </item>

    <!-- Tu forma -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="5dp" />
            <padding
                android:left="7dp"
                android:top="7dp"
                android:right="7dp"
                android:bottom="7dp" />
        </shape>
    </item>

</layer-list>