<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:fitsSystemWindows="true">
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@string/role_description"
            android:scaleType="centerCrop"
            android:src="@drawable/image_dark_village" />

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/maxAmountTextViewContainer"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_marginTop="20dp"
            app:layout_constraintBottom_toTopOf="@id/recyclerView"
            android:layout_width="100dp"
            android:layout_height="70dp"
            app:cardElevation="0dp"
            app:strokeColor="@color/dark_red"
            app:strokeWidth="5dp"
            app:shapeAppearanceOverlay="@style/corner10dp">

            <TextView
                android:id="@+id/maxAmountTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/image_wood_background_clean"
                android:padding="6dp"
                android:gravity="center"
                android:fontFamily="@font/beleren2016_bold"
                android:textColor="@android:color/black"
                android:text="@string/max"
                android:textSize="18sp"/>

        </com.google.android.material.card.MaterialCardView>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="580dp"
            android:layout_margin="10dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toTopOf="@id/doneButton"
            app:layout_constraintTop_toBottomOf="@id/maxAmountTextViewContainer">

        </androidx.recyclerview.widget.RecyclerView>

        <Button
            android:id="@+id/doneButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:layout_margin="20dp"
            android:text="@string/done"
            android:layout_gravity="bottom|center_horizontal"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:fontFamily="@font/beleren2016_bold"
            />

    </androidx.constraintlayout.widget.ConstraintLayout>
</FrameLayout>